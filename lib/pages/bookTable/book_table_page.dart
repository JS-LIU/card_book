import 'package:flutter/material.dart';
import 'package:flutter/cupertino.dart';

class BookTablePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("booktablepage"),
    );
  }
}